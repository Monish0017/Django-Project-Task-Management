python3.11 -m venv Django-env

source Django-env/bin/activate

install the requirements . then pip freeze > requirements.txt . Requirements.txt have all the modules we used for this project

django-admin startproject config .
Project created with title config , then . means don't another dir for project

python manage.py startapp accounts 
python manage.py startapp tasks

inside settings.py I added the cors origin , Cookies etcc.., Additional I did for this project

# To run this project

python manage.py runserevr




# To run React JS
cd frontend
npm i
npm run dev

This will start reactvite app inside (http://localhost:5173/)

Register your account then login to that .
You can see the credentials as response inside the network tab





